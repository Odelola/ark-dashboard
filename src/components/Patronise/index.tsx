import React, { FC } from 'react';
// import  GoogleApiWrapper  from "../index"
import "./Patronise.css"

interface PatroniseProps {

}

const Patronise: FC<PatroniseProps> = ({ }) => {
    return (
        <div>MAP</div>
    )
}
export default Patronise;